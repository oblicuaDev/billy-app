import { View } from "react-native";
import React from "react";

const Separator = () => {
  return (
    <View style={{ width: "100%", backgroundColor: "#B0B0B0", height: 1 }} />
  );
};

export default Separator;
